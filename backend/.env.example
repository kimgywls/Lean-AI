# Django 설정
DJANGO_SECRET_KEY=your-secret-key-here
DEBUG=True

# 데이터베이스 설정 (SQLite)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=your_database_name.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# PostgreSQL 사용 시 (SQLite 대신)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=your_database_name
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=localhost
# DB_PORT=5432

# 데이터베이스 경로
DATABASE_PATH=your_database_name.sqlite3
BACKUP_DIR=backups

# 이메일 설정 (Gmail)
GMAIL_APP_PASSWORD=your-gmail-app-password
GMAIL_ACCOUNT=your-email@gmail.com

# Slack 웹훅
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# 보안 설정
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://your-domain.com
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:3001,https://your-domain.com
